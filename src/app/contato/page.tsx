'use client'

import { Contact } from '@/components/ui/contact'

export default function Contato() {
  return (
    <main className="mt-36 flex min-h-screen flex-col items-center justify-between bg-background px-4">
      <Contact />
    </main>
  )
}
